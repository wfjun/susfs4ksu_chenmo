name: Sync gki-android15-6.6 from simonpunk

on:
  workflow_call:

jobs:
  call-sync:
    uses: ./.github/workflows/sync-upstream.yml
    with:
      upstream_repo: https://gitlab.com/simonpunk/susfs4ksu.git
      upstream_branch: gki-android15-6.6
      local_branch: gki-android15-6.6
